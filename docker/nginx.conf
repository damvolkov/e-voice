map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Route /v1/ to API (HTTP) or WS server (WebSocket) based on Upgrade header
map $http_upgrade $v1_upstream {
    default api;
    "websocket" ws;
}

upstream api {
    server 127.0.0.1:5500;
}

upstream ws {
    server 127.0.0.1:5700;
}

upstream ui {
    server 127.0.0.1:5600;
}

server {
    listen 80;
    server_name _;

    client_max_body_size 100M;

    # API + WebSocket — HTTP goes to Robyn, WS upgrades go to standalone server
    location /v1/ {
        proxy_pass http://$v1_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
    }

    location /health {
        proxy_pass http://api;
    }

    # Robyn OpenAPI docs
    location /docs {
        proxy_pass http://api;
        proxy_set_header Host $host;
    }

    location /openapi.json {
        proxy_pass http://api;
    }

    # Everything else → Gradio UI
    location / {
        proxy_pass http://ui;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $http_host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
    }
}
